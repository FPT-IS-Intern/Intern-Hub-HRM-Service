server:
  port: 8080

security:
  internal-secret: ${INTERNAL_SECRET_KEY:defaultSecretValue}
  internal-path-prefix: /hrm-service/internal/

# Optional: JPA Auditing configuration
audit:
  data:
    # Enable/disable JPA auditing (default: true)
    enabled: true
    # Default user ID for system operations when no user is authenticated (default: 0)
    default-system-id: 0

snowflake:
  machine-id: 1

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}?currentSchema=${SCHEMA_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      max-lifetime: 300000
      idle-timeout: 120000
      keepalive-time: 60000
      validation-timeout: 5000
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: hrm

services:
  gateway:
    url: ${GATEWAY_URL:http://localhost:8765}

feign:
  client:
    config:
      auth:
        url: ${AUTH_SERVICE_URL:http://localhost:8888}

cloudflare:
  r2:
    access-key-id: ${ACCESS_KEY_ID}
    secret-key-id: dab77eec28a218c479ba35fc903b4b6346aad1b356cd3736651c92cf8142d488
    end-point: https://f452486d5bdeefd2b7464d3bd3870fd8.r2.cloudflarestorage.com
    bucket-name: intern-hub
    public-base-url: https://pub-7bdfb6524c844f47bcec04620ec6c03a.r2.dev